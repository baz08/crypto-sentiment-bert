# Test dependencies. Deliberately excludes transformers/tensorflow: the API
# tests mock the BERT model instead of loading real weights, so the suite
# runs in seconds without a multi-GB download.
pytest==7.4.4
httpx==0.27.0
fastapi==0.92.0
uvicorn==0.20.0
pydantic==1.10.5

pandas==2.1.4
scikit-learn==1.4.0
nltk==3.8.1
contractions==0.1.73
matplotlib==3.8.4
gradio==6.24.0
