# Test dependencies. Deliberately excludes transformers/tensorflow: the API
# and demo tests mock the BERT model instead of loading real weights, so the
# suite runs in seconds without a multi-GB download.
#
# fastapi/pydantic/uvicorn are intentionally left unpinned here (unlike
# deployment/api/requirements.txt, which pins fastapi==0.92.0/
# pydantic==1.10.5 to match what's actually deployed): gradio requires
# fastapi>=0.115.2 and pydantic 2.x, which conflicts with those older pins.
# The tests only assert on status codes and JSON bodies, which are stable
# across that version gap.
pytest==7.4.4
httpx==0.27.0
fastapi
uvicorn
pydantic

pandas==2.1.4
scikit-learn==1.4.0
nltk==3.8.1
contractions==0.1.73
matplotlib==3.8.4
gradio==6.24.0
